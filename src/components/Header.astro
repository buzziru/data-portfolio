---
// Nav links are absolute (/#id) so they also work from project detail pages.
// 섹션 번호는 쓰지 않는다 — 이름만 (DESIGN-instrument.md).
const links = [
  { href: '/#thesis', label: 'THESIS' },
  { href: '/#work', label: 'WORK' },
  { href: '/#stack', label: 'STACK' },
  { href: '/#log', label: 'LOG' },
];
---

<header class="top">
  <div class="top-inner">
    <a class="brand" href="/">
      <span class="brand-name">CHO INKYOUNG</span>
      <span class="brand-sub">DATA ANALYST / MEASUREMENT LOG</span>
    </a>
    <nav class="top-nav" aria-label="Primary">
      {links.map((l) => <a href={l.href}>{l.label}</a>)}
      <a class="nav-cta" href="/#contact">CONTACT</a>
    </nav>
  </div>
</header>
